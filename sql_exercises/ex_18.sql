SELECT SUM(i.Total), e.FirstName || " " || e.LastName FROM Employee e JOIN Customer c ON e.EmployeeId == c.SupportRepId JOIN Invoice i ON c.CustomerId == i.CustomerId WHERE strftime("%Y", i.InvoiceDate)== "2009" GROUP BY e.EmployeeId ORDER BY SUM(i.Total) DESC LIMIT 1;
